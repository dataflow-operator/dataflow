# Быстрый старт с DataFlow MCP Server

Краткое руководство для быстрого начала работы.

## 1. Установка (выберите один вариант)

### Вариант A: Docker (рекомендуется)

```bash
# Скачайте образ
docker pull ghcr.io/ilyario/mcp-dataflow:latest

# Или соберите сами
cd mcp-dataflow
task docker-build
```

### Вариант B: Бинарник

```bash
cd mcp-dataflow
task build
# Бинарник будет в текущей директории: ./mcp-dataflow
```

## 2. Настройка в Cursor

Добавьте в настройки Cursor (Settings → Features → MCP):

**Для Docker:**
```json
{
  "mcpServers": {
    "dataflow": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "-v", "${HOME}/.kube/config:/root/.kube/config:ro",
        "mcp-dataflow:latest"
      ]
    }
  }
}
```

**Для бинарника:**
```json
{
  "mcpServers": {
    "dataflow": {
      "command": "/path/to/mcp-dataflow",
      "args": ["--kubeconfig", "${HOME}/.kube/config"]
    }
  }
}
```

## 3. Перезапуск Cursor

Перезапустите Cursor для применения изменений.

## 4. Проверка работы

Откройте чат с AI-ассистентом и попробуйте:

```
Покажи все DataFlow ресурсы
```

или

```
Создай DataFlow из Kafka топика 'events' в PostgreSQL таблицу 'processed_events'
```

## Что дальше?

- 📖 [Полная инструкция по установке](INSTALLATION.md)
- 📚 [Руководство по использованию](USAGE.md)
- 🔧 [Документация проекта](../docs/en/index.md)

## Проблемы?

См. раздел "Устранение неполадок" в [INSTALLATION.md](INSTALLATION.md)
